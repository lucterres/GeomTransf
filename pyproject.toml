[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "geomtransf"
version = "0.1.0"
description = "Geometry Transformation Tool"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
geomtransf = "src.main:main"
